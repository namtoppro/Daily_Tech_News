# Daily Tech News 품질 규칙

## 최우선 원칙
- 글자 수보다 **기사의 핵심 정보 보존**이 우선이다.
- 카드 길이를 맞추기 위해 핵심 사실, 수치, 날짜, 주체, 영향이 누락되면 안 된다.
- 짧게 줄이더라도 독자가 `무슨 일이 있었는지`, `왜 중요한지`를 이해할 수 있어야 한다.

## 본문 요약 규칙
각 기사 요약에는 가능한 한 아래 요소가 유지되어야 한다.
1. 누가 발표/행동했는가
2. 무엇이 바뀌었는가
3. 수치/규모/날짜가 있으면 포함
4. 왜 중요한가
5. 필요한 경우 어떤 영향이 있는가

## 인사이트 규칙
- 인사이트는 본문과 섞지 말고 **줄바꿈 후 `[인사이트]`** 로 시작한다.
- 인사이트는 사실처럼 단정하지 말고 해석/시사점 수준으로 쓴다.
- 인사이트를 넣더라도 본문 사실 요약이 먼저 충분히 서술되어야 한다.
- `[인사이트]`를 넣기 위해 본문 사실이 과도하게 압축되면 안 된다.

## 길이 규칙
- 길이 제한은 `권장치`일 뿐, 핵심 내용 보존보다 우선하지 않는다.
- HEADLINE, MAJOR, BRIEF 모두 핵심 사실이 유지되지 않으면 길이를 늘릴 수 있다.
- 특히 HEADLINE은 카드 균형보다 정보 완전성을 우선한다.

## 금지
- 카드 길이를 맞추기 위해 핵심 내용을 삭제하는 것
- `누가/무엇을/왜` 중 하나가 빠진 요약
- 수치가 핵심인 기사에서 숫자를 빼는 것
- 과장된 전망을 위해 사실 요약을 줄이는 것

## 점검 체크리스트
발행 전 아래를 확인한다.
- 본문만 읽어도 핵심 사실이 이해되는가?
- `[인사이트]`를 제거해도 기사 요약이 성립하는가?
- 수치/날짜/주체가 누락되지 않았는가?
- 인사이트가 사실을 덮어쓰지 않는가?

## 오디오 확장 규칙
- 오디오 기능 추가 전에는 반드시 복원 가능한 git 태그 백업을 만든다.
- 오디오 파이프라인은 `텍스트 뉴스 발행`과 분리한다. 오디오 생성 실패 때문에 HTML/Markdown 발행이 실패하면 안 된다.
- 기본 순서는 `텍스트 발행 -> 오디오 대본 생성 -> 음성 파일 생성`이다.
- 오디오용 대본은 뉴스 본문의 핵심 사실을 보존해야 하며, 과장되거나 방송형 과장이 심하면 안 된다.
- NotebookLM 스타일의 자연스러운 오디오 브리핑은 허용하지만, 사실과 해석은 구분되어야 한다.
- TTS 모델/SDK가 불안정하면 텍스트 발행은 유지하고 오디오 단계만 실패 처리한다.
- 오디오 파일명은 날짜 기준으로 일관되게 저장한다. 예: `archive/YYYY-MM-DD.mp3`, `archive/YYYY-MM-DD-audio-script.md`
- 코드 수정 중 새로 확정된 운영 규칙은 이 파일에 먼저 기록하고 반영한다. 그래야 이후 수정 과정에서 원복/누락되지 않는다.

## 이미지 생성 및 영상 확장 규칙
- 이미지 기능 추가 전에도 반드시 복원 가능한 git 태그 백업을 만든다.
- 이미지 생성 파이프라인은 `텍스트 뉴스 발행`, `오디오 생성`과 분리한다. 이미지 실패 때문에 텍스트/오디오 발행이 실패하면 안 된다.
- 이미지 생성은 `gemini-3.1-flash-image-preview` 기준으로 설계한다.
- 이미지 생성은 병렬이 아니라 **직렬 처리**를 기본으로 한다.
- 분당 생성 제한 가능성을 고려해 하루 생성 수는 기본 **최대 6장**으로 제한한다.
- 우선순위는 `HEADLINE 2개 + MAJOR 3~4개` 기준으로 잡는다.
- 각 이미지 생성 호출 사이에는 대기 시간을 둔다. 기본 권장치는 5초다.
- 429/limit 오류가 나면 재시도는 제한적으로만 하고, 계속 실패하면 해당 이미지는 스킵하거나 fallback 이미지를 사용한다.
- 이미지 프롬프트는 기사 원문 복붙이 아니라 `핵심 장면`, `분위기`, `시각 키워드`, `금지 요소`를 정리한 전용 프롬프트로 생성한다.
- 이미지 안에 과한 텍스트, 로고, 워터마크, 선정적 표현은 금지한다.
- 영상(mp4) 생성 시 이미지 실패분이 있어도 전체 파이프라인이 멈추지 않게 fallback 이미지를 허용한다.
- 생성 파일명은 날짜 기준으로 고정한다. 예: `archive/YYYY-MM-DD-img-01.png`, `archive/YYYY-MM-DD.mp4`
- mp4 생성은 `오디오 길이` 기준으로 이미지 표시 시간을 자동 분배한다.
- 영상은 화려한 효과보다 안정적인 정지 이미지 슬라이드쇼를 우선한다.
- 영상 생성 실패도 텍스트/오디오 발행을 깨면 안 된다.
- 유튜브 업로드는 외부 게시이므로 별도 인증/승인 단계로 분리한다.

## 하단 해시태그 규칙
- 하단 해시태그에는 기본적으로 `https://lowprice.koreall.site/` 와 `#청담랩` 을 포함한다.
- 기존 기술/AI 해시태그가 있어도 위 두 태그는 누락되면 안 된다.
